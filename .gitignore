.venv
**/__pycache__
*venv
.venv*
schema.db
*.db
.env

# Scratch de execução de planos (subagent-driven development)
.superpowers/

# Arquivos enviados vivem no disco local (ADR-003) e nunca são versionados.
# As exceções mantêm a estrutura de pastas no repositório.
uploads/**
!uploads/receipts/
!uploads/receipts/.gitkeep
!uploads/avatars/
!uploads/avatars/.gitkeep
!uploads/payment_receipts/
!uploads/payment_receipts/.gitkeep

# Relatórios de cobertura (Item 26) — gerados, nunca versionados.
.coverage
htmlcov/
